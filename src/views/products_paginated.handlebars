  <div class="d-flex justify-content-between align-items-center p-4">
         <h1>Productos paginados</h1>
         <a type="button" class="btn btn-secondary " href="/create"><span class="h4">+</span> Agregar nuevo</a>
   </div>
   <div class="p-3">
      <form id="form-querys " >
          {{!--   <div class="col-4  mb-3">
             <div class="form-check form-check-inline">
               <input class="form-check-input" type="radio" name="radioOrder" id="ascendent" value=1>
               <label class="form-check-label" for="ascendent">
                 Ordenar por menor precio
               </label>
               </div>
               <div class="form-check form-check-inline">
               <input class="form-check-input" type="radio" name="radioOrder" id="descendet" value=-1>
               <label class="form-check-label" for="descendet">
                  Ordenar por mayor precio
               </label>
               </div> 
            </div>--}}
            <div class="bg-warning p-2 d-flex gap-3">
           
               {{!-- <label for="page" class="form-label">Ver pagina</label> --}}
               <input type="number" name="page"  class="form-control" id="page" placeholder="ingrese número de página." value="page">
           
               {{!-- <label for="limit" class="form-label">ver limite</label> --}}
               <input type="number" name="limit"  class="form-control" id="limit" placeholder="ingrese cantidad de articulos deseados" value="limit">
           
               <button type="submit" class="btn btn-primary">Buscar</button>
               
               </div>
      </form>
   </div>
   <div class="row d-flex justify-content-center p-4">
          <table class="table-responsive table table-striped table-bordered" >
           
            <thead>
               <tr>
                  <th scope="col">Codigo</th>
                  <th scope="col">Producto</th>
                  <th scope="col">Descripcion</th>
                  <th scope="col">Precio</th>
                  <th scope="col">Categoria</th>
                  <th scope="col">Stock</th>
                  <th scope="col">Acciones</th>
               </tr>
            </thead>
            <tbody id="tableProductsBody">
               {{#each docs}}
               <tr>
                  <th scope="row">{{this.code}}</th>
                  <td>{{this.title}}</td>
                  <td>{{this.description}}</td>
                  <td>{{this.price}}</td>
                  <td>{{this.category}}</td>
                  <td>{{this.stock}}</td>
                  <td>  
                     <a type="button" class="btn btn-primary w-100 mb-1" href="/product/{{this.code}}">Ver</a>
                     <a type="button" class="btn btn-success w-100 mb-1" href="/api/carts/carts/64d010993021e5b79249df1f/{{this._id}}">Agregar al carrito</a>
                     <a type="button" class="btn btn-warning w-100 mb-1" href="/update/{{this.code}}">Modificar<a>
                     <a type="button" class="btn btn-danger w-100 mb-1" href="/api/products/delete/{{this.code}}">Eliminar</a>
                   </td>
               </tr>
                  {{/each}}
          </table>
</div>
<script>
   document.getElementById('form-querys').onsubmit=(e)=>{
      e.preventDefault()
    
      const limit= document.getElementById('limit').value
      const page= document.getElementById('page').value
     // const sort= document.getElementsById("ascended")
    //  if(sort.checked){
   //      console.log=("esta chequeado", sort.value)
    //  }
    //  console.log(limit, page)
      const url=`/products_paginated?limit=${limit}&page=${page}`
      window.location.href=url
   }

</script>